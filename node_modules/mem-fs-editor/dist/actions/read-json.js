export default function readJSON(filepath, defaults) {
    if (this.exists(filepath)) {
        try {
            const content = this.read(filepath);
            return JSON.parse(content);
        }
        catch (error) {
            throw new Error('Could not parse JSON in file: ' +
                filepath +
                '. Detail: ' +
                error.message, {
                cause: error,
            });
        }
    }
    else {
        return defaults;
    }
}
