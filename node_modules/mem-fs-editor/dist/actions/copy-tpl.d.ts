import ejs from 'ejs';
import type { MemFsEditor } from '../index.ts';
type CopyTplOptions = Omit<NonNullable<Parameters<MemFsEditor['copy']>[2]>, 'fileTransform' | 'transformData'> & {
    transformOptions?: ejs.Options;
};
type CopyTplParameters = [
    from: string | string[],
    to: string,
    data?: ejs.Data,
    options?: CopyTplOptions,
    compatOptions?: Omit<NonNullable<Parameters<MemFsEditor['copy']>[2]>, 'fileTransform' | 'transformData'>
];
export declare function copyTpl(this: MemFsEditor, ...[from, to, data, options, compatOptions]: CopyTplParameters): void;
export {};
